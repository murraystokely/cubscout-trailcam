# Downloaded model weights (~280 MB for MDv5a) and everything derived from
# the photographs: the manifest, crops, exports.  All reproducible from the
# photo library plus `python3 -m trailcam scan && ... detect`.
ai/models/
ai/data/
ai/.venv/

__pycache__/
*.pyc

# The benchmark corpus: 118 MB of frames, rebuildable with `bench build`
# from the seed recorded in its own corpus.json.  The RESULTS are committed
# (ai/results/benchmarks/*.json) -- they are findings, and they are small.
ai/data/bench/
